<UserControl x:Class="Player.Controls.MediaPlayer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" MouseMove="Element_MouseMove"
             xmlns:local="clr-namespace:Player.Controls"
             mc:Ignorable="d" Loaded="UserControl_Loaded" x:Name="ParentControl"
             d:DesignHeight="300" d:DesignWidth="600">
	<Grid>
		<Grid Background="Black" x:Name="elementCanvas" Margin="200,500,200,0">
			<MediaElement x:Name="element" LoadedBehavior="Manual" UnloadedBehavior="Stop" 
                          MouseDown="Element_MouseDown" ScrubbingEnabled="True"
                          Volume="1"/>
		</Grid>
		<Grid x:Name="ControlsGrid" Background="Transparent" VerticalAlignment="Top" Height="87">
			<Label Content="Content Title" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="67,0,0,0" FontSize="18" x:Name="TitleLabel" Foreground="{DynamicResource Foreground}"/>
			<Slider x:Name="PositionSlider" ValueChanged="Position_Changed" PreviewMouseLeftButtonDown="Position_Holding" IsMoveToPointEnabled="True"
               VerticalAlignment="Center" Maximum="1000" Focusable="False" FocusVisualStyle="{x:Null}" Template="{DynamicResource FlatSlider}"
                        Foreground="#FF101010" Margin="50, 62, 50, 0">
			</Slider>
			<Label Content="00:00" Style="{DynamicResource TimeLabel}" HorizontalAlignment="Left" x:Name="TimeLabel_Current" Margin="10,0,0,0"/>
			<Label Content="00:00" Style="{DynamicResource TimeLabel}" HorizontalAlignment="Right" x:Name="TimeLabel_Full" Width="38" Margin="0,0,10,0"/>
			<local:MaterialButton x:Name="PreviousButton" Glyph="Previous" Margin="87,0,0,25" MouseUp="PreviousButton_Clicked" />
			<local:MaterialButton x:Name="NextButton" Glyph="Next" Margin="172,0,0,25" MouseUp="NextButton_Clicked" />
			<local:MaterialButton x:Name="PlayPauseButton" Glyph="Play" MouseUp="PlayPauseButton_Clicked" Margin="127,0,0,24" Height="24" Width="24"/>
			<local:MaterialButton x:Name="VisionButton" Glyph="Video" HorizontalAlignment="Center" Margin="0,0,0,24" MouseUp="VisionButton_Clicked" />
			<local:MaterialButton x:Name="PlayModeButton" Glyph="RepeatAll" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,197,29" EllipseType="Rectular" MouseUp="PlayMode_Click"/>
			<local:MaterialButton x:Name="FullScreenButton" Glyph="FullScreen" HorizontalAlignment="Right" Margin="0,0,10,29" VerticalAlignment="Bottom" MouseUp="FullScreenButton_Clicked" />
			<local:MaterialIcon x:Name="VolumeIcon" Glyph="Volume3" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0, 0, 155, 29" Foreground="{DynamicResource Foreground}" />
			<Slider x:Name="VolumeSlider" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,28,50,26" IsMoveToPointEnabled="True"  Template="{DynamicResource FlatSlider}" Foreground="#F444"
                    Width="100" Maximum="99" SmallChange="2" LargeChange="10" AutoToolTipPlacement="TopLeft" ValueChanged="VolumeSlider_ValueChanged"/>
			<Grid.RenderTransform>
				<TranslateTransform x:Name="ControlsTranslation" Y="0" />
			</Grid.RenderTransform>
		</Grid>
	</Grid>
	<UserControl.Resources>
		<SolidColorBrush x:Key="Foreground" Color="Black" />
		<Style TargetType="{x:Type Label}" x:Key="TimeLabel">
			<Setter Property="Foreground" Value="{DynamicResource Foreground}" />
			<Setter Property="VerticalAlignment" Value="Bottom"/>
			<Setter Property="Width" Value="40" />
			<Setter Property="RenderTransform">
				<Setter.Value>
					<TranslateTransform x:Name="Translation1" Y="0"/>
				</Setter.Value>
			</Setter>
		</Style>
		<Style TargetType="local:MaterialButton" >
			<Setter Property="Foreground" Value="{DynamicResource Foreground}" />
			<Setter Property="VerticalAlignment" Value="Bottom" />
			<Setter Property="HorizontalAlignment" Value="Left"/>
		</Style>
		<Storyboard x:Key="MinifyBoard" Storyboard.TargetName="elementCanvas" AccelerationRatio="0.4" DecelerationRatio="0.6" SpeedRatio="1">
			<ThicknessAnimation From="{Binding ElementName=elementCanvas, Path=Margin}" To="200,500,200,0" Storyboard.TargetProperty="Margin" />
		</Storyboard>
		<Storyboard x:Key="MagnifyBoard" Storyboard.TargetName="elementCanvas" AccelerationRatio="0.4" DecelerationRatio="0.6" SpeedRatio="1">
			<ThicknessAnimation From="{Binding ElementName=elementCanvas, Path=Margin}" To="0" Storyboard.TargetProperty="Margin" />
		</Storyboard>
		<Storyboard x:Key="FullOnBoard" Storyboard.TargetProperty="Y" Storyboard.TargetName="ControlsTranslation" AccelerationRatio="0.9" DecelerationRatio="0.1">
			<DoubleAnimation From="{Binding ElementName=ControlsTranslation, Path=Y}" To="80" />
		</Storyboard>
		<Storyboard x:Key="FullOffBoard" Storyboard.TargetProperty="Y" Storyboard.TargetName="ControlsTranslation" AccelerationRatio="0.1" DecelerationRatio="0.9">
			<DoubleAnimation From="{Binding ElementName=ControlsTranslation, Path=Y}" To="0" />
		</Storyboard>
		<TranslateTransform x:Name="Translation1" x:Key="Translation1" Y="0" />
		<ControlTemplate TargetType="Slider" x:Key="FlatSlider">
			<Border x:Name="border" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}" SnapsToDevicePixels="True">
				<Grid>
					<Track x:Name="PART_Track" Grid.Row="1" MouseDown="Position_Holding">
						<Track.DecreaseRepeatButton>
							<RepeatButton Height="9" Background="{TemplateBinding Foreground}" />
						</Track.DecreaseRepeatButton>
						<Track.IncreaseRepeatButton>
							<RepeatButton Height="5"/>
						</Track.IncreaseRepeatButton>
						<Track.Thumb>
							<Thumb x:Name="Thumb" OverridesDefaultStyle="True" >
								<Thumb.Template>
									<ControlTemplate TargetType="{x:Type Thumb}">
										<Grid>
											<Ellipse Width="15" x:Name="grip" StrokeThickness="2" Stroke="{TemplateBinding Foreground}" Fill="#01FFFFFF"
                                                          Height="3" Margin="-2, 0, 0, 0"/>
										</Grid>
										<ControlTemplate.Triggers>
											<Trigger Property="IsMouseOver" Value="true">
												<Setter Property="StrokeThickness" TargetName="grip" Value="3"/>
											</Trigger>
											<Trigger Property="IsDragging" Value="true">
												<Setter Property="StrokeThickness" TargetName="grip" Value="10" />
											</Trigger>
											<EventTrigger RoutedEvent="UIElement.MouseEnter">
												<BeginStoryboard>
													<Storyboard SpeedRatio="4" AccelerationRatio="0.2" DecelerationRatio="0.8" Storyboard.TargetName="grip">
														<DoubleAnimation From="15" To="20" Storyboard.TargetProperty="Width"/>
														<DoubleAnimation From="3" To="20" Storyboard.TargetProperty="Height"/>
													</Storyboard>
												</BeginStoryboard>
											</EventTrigger>
											<EventTrigger RoutedEvent="UIElement.MouseLeave">
												<BeginStoryboard>
													<Storyboard SpeedRatio="3" AccelerationRatio="0.2" DecelerationRatio="0.8" Storyboard.TargetName="grip">
														<DoubleAnimation From="20" To="15" Storyboard.TargetProperty="Width"/>
														<DoubleAnimation From="20" To="3" Storyboard.TargetProperty="Height"/>
													</Storyboard>
												</BeginStoryboard>
											</EventTrigger>
										</ControlTemplate.Triggers>
									</ControlTemplate>
								</Thumb.Template>
							</Thumb>
						</Track.Thumb>
					</Track>
				</Grid>
			</Border>
		</ControlTemplate>
	</UserControl.Resources>
</UserControl>
